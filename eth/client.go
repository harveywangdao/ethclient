package eth

/*import (
	"fmt"
	"github.com/ethereum/go-ethereum/rpc"
	"log"
)

func sdddd() {
	client, err := rpc.Dial("http://localhost:8545")
	if err != nil {
		log.Fatalln(err)
	}

	var account []string
	err = client.Call(&account, "eth_accounts")
	if err != nil {
		log.Fatalln(err)
	}

	var balance string
	err = client.Call(&balance, "eth_getBalance", account[0], "latest")
	if err != nil {
		log.Fatalln(err)
	}

	fmt.Println(account, balance)
}
*/
